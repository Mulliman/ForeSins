<span class="fs-horny @(IsLarge ? "fs-horny--large" : "") @(IsSmall ? "fs-horny--small" : "")">@Text</span>

@code {
    [Parameter] public string Text { get; set; }

    [Parameter] public bool IsLarge { get; set; }

    [Parameter] public bool IsSmall { get; set; }
}